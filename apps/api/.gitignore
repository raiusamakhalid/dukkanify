node_modules
# Keep environment variables out of version control
.env

# Prisma 7 emits the client into the source tree; it is a build artefact, not source.
# Regenerated by the `postinstall` script on every install.
src/generated
